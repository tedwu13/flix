<header id="content-header">
  <h1>New Review for <%= link_to(@movie.title, @movie) %></h1>
</header>
<%= form_for([@movie, @review]) do |f| %>
  <fieldset>
    <%= render 'shared/errors',object: @review %>
    <ol>
      <li class="required">
        <%= f.label :name %><br/>
        <%= f.text_field :name, size: 40, autofocus: true %>
      </li>

      <li class="required stars">
        <%= f.label :stars %><br/>
        <%= f.select :stars, Review::STARS, prompt: "Pick a star" %>
      </li>

      <li class="required">
        <%= f.label :comment %><br/>
        <%= f.text_area :comment, cols: 80, placeholder: "What did you think?" %>
      </li>
    </ol>
    <p>
      <%= f.submit 'Post Review' %>
      <%= link_to "Cancel Review", @movie, class: 'button' %>
    </p>
  </fieldset>
<% end %>
